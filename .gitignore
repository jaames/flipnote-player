# we're compiling scss -> css so no need to have css in source
src/**/*.css
# sample flipnote .ppms shouldn't be in source
*.ppm

# dependencies
/node_modules

# testing
/coverage

# production
/build

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
